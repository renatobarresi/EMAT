/*
 * project_cpp.cpp
 *
 *  Created on: May 14, 2023
 *      Author: renatobarresi
 */

#include "project_cpp.h"

/* Includes for testing purposes */
#include "filesystem.h"
#include "error.h"

void main_cpp(void)
{
	/* Test writing and reading of a file*/
	/*fileSystem& fs = fileSystem::getInstance();

	const char file[] = "helloLittleFS.txt";

	fs.write(file, "hello World! 123");

	char readBuff[128];
	fs.read(file, readBuff, 16);*/

	/* Test assert */
	//assert(1 == 0);

	/* test the error handler */
	errorHandler(__FILE__, __LINE__, ERROR_FATAL, "NULL POINTER");

	while(1);
}

